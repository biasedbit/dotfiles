# fallback to legacy version files (e.g. `.ruby-versions`) if present
legacy_version_file = no

# Keep all downloads for convenience when working offline
always_keep_download = yes

# https://github.com/halcyon/asdf-java?tab=readme-ov-file#macos
java_macos_integration_enable = yes
